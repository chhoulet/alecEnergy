{% extends "::baseAdmin.html.twig" %}

	{% block title %}

		Editer {{ article.title|title }}

	{% endblock %}

	{% block body %}

		<div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">
                	Mise à jour {{ article.title|title }}                    
                </h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>

        <div class="row">
        	<div class="col-lg-12">
        		{{ tinymce_init() }}
				{{ form_start(form)}}
				{{ form_row(form.title)}}
				{{ form_row(form.subject)}}
				{{ form_row(form.main)}}				
				<div style="text-align:center;margin-top:25px;">
					{{ form_row(form.dateCreated)}}
				</div>
				<div style="text-align:center;margin-top:25px;">
					{{ form_row(form.dateDeleted)}}
				</div>
				<div class="col-md-6" style="text-align:center;margin-top:25px;margin-bottom:25px;">
					{{ form_row(form.submit)}}
				</div>
				<div class="col-md-6" style="text-align:center;margin-top:25px;margin-bottom:25px;">
					{{ form_row(form.saveAndAdd)}}
				</div>
				{{ form_end(form)}}

			</div>
		</div>

	{% endblock %}